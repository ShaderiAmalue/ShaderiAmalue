{
  "version": 2,
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ],
  "builds": [
    { "src": "api/hello.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/hello.js" }
  ]
}